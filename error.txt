# OLD — appended blindly every run
merged_content = base_content + separator + added_content

# NEW — deduplicates before appending
existing_normalized = {str(line).strip().lower() for line in base_content}
new_lines = []
for line in incoming_content:
    normalized = str(line).strip().lower()
    if normalized and normalized not in existing_normalized:
        new_lines.append(line)
        existing_normalized.add(normalized)   # also dedupes within Space 2

if new_lines:
    merged_content = base_content + ["\r\n"] + new_lines
else:
    merged_content = base_content   # nothing new — no change



# OLD — used Space 2's original title
"title": outer2["title"]

# NEW — uses NEW_SPACE_TITLE from Cell 1 config
"title": NEW_SPACE_TITLE
