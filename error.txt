# ── Run merge ─────────────────────────────────────────────────────────────────
print("\n⏳ Merging...")
merged_ss, report = merge_spaces(ss1, ss2)

# ── Post-merge: sort tables alphabetically (API strictly requires this) ────────
merged_ss["data_sources"]["tables"] = sorted(
    merged_ss.get("data_sources", {}).get("tables", []),
    key=lambda t: t.get("identifier", "") if isinstance(t, dict) else ""
)
print("  ✅ Tables sorted by identifier.")
